import { Lato, Outfit } from 'next/font/google';

export const lato = Lato({
    subsets: ['latin'],
    weight: ['300', "400", "700"]
});

export const outfit = Outfit({
    subsets: ['latin'],
    weight: ['400', '500', '600'],
});

